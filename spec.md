\#dataTable pro-项目规格说明书（spec.md)

1 .       核心模块                                       技术实现规格                                                                                                                                  设计目标/优势

&#x20;      类型系统与架构                               实现完整TS泛型定义<Data Table<T>>,严格类型推导与属性校验，杜绝any类型隐患                   极致类型安全，提升开发体验

&#x20;      组件结构设计                                   采用compound组合模式拆分Header/Body/Pagination,支持自定义插槽与灵活组装                      解耦UI结构，适配多样化布局

&#x20;      交互与状态控制                                支持受控/非受控双模式，内置列表排序/筛选/显隐，暴露选中行hook与批量操作能力             覆盖百分之90业务交互，开箱即用

&#x20;      数据与生态集成                                 抽离use dateTable业务逻辑hook，深度集成React Query实现服务端数据联动与缓存                  无缝衔接数据层，减少重复逻辑



2\.质量验收标准：Storybook组件案例覆盖≥6个场景

单元测试覆盖率≥60%，无语法报错

撰写完整README，含API文档与示例成功发布至NPM Public源，支持按需引入

3核心技术选型：

·语言:TypeScript(Strict严格模式)

·框架:React18+，使用Hooks封装逻辑

·构建:Rollup/Vite打包，支持ESModule

·测试: Jest+React Testing Library

4\.项目价值与产出：统一5条产品线表格标准，消除重复造轮子·沉淀团队通用基建资产，提升研发效能30%·简历核心亮点:主导开源组件库设计与发布·降低维护成本，形成可复用的技术资产

5\.易踩坑8条·提前打疫苗

AI写Compound+泛型+RQ时，这8个坑必踩，看完再开干：

01\.Compound漏Context默认值：组件单独使用直接崩溃，需设createContextkThnull>(nul)，并在自定义Hook中抛出友好错误提示。

02.泛型未透传至内部Hook ：仅在Props标注泛型会导致Hook退化为any，需保证JseDataTable<T>(与组件泛型保持一致。

03.选择状态双数据源冲突:同时接收外部selectedRows和内部useState会导致状态不同步，需判断是否传值来决定受控模式。

04.QueryKey引用不稳定：每次渲染生成新对象字面量会触发重复请求，需用useMemo缓存或扁平化处理查询参数。

05.Storybook过度追求规范：30分钟写不出一个Story，应遵循“先丑后美”原则，先实现核心功能展示，再逐步完善规范。

06.单测盲目追求100%覆盖：首日目标60%即可，优先覆盖useDataTable核心逻辑与边界场景，拒绝为覆盖率而测试。

07.npm发布漏关键配置字段：package.json必须齐main、module、types、files、exports字段,否则包无法被正确引用。

08.README只写用法无卖点：不仅要写“怎么装怎么用”，更要写“为何选它而非antdTable"补充设计权衡与核心优势。

6\.阶段交付·每阶段都有验收点

严禁AI一次性吐300行·分阶段开发+学员审核+功能跑通闭环推进

阶段标识                      核心任务                                                 验收标准

&#x20;S1项目基建                搭建骨架+tsup+TS+SB初始化               pnpm storybook启动成功

S2-S3核心构建           类型定义+骨架+Hook封装(排序)           组件渲染+排序功能演示通过

S4-S5功能增强           选择器双模式+ReactQuery集成             双模式切换+Mock数据拉取成功

S6-S7验收交付           核心单测+文档编写+部署发布                覆盖率≥60%+可发布/Demo上线

7\.交付心法:分阶段拆解是降低复杂度的核心!拒绝A一次性生成海量代码，必须严格执行「阶段验收、学员确认、功能跑通」的铁三角标准，确保每一步工程质量可控，避免“一次性写完再返工”的低效陷阱。

8\.核心功能

1.支持传入任意数据(TypeScript自动推导字段名)

2.用复合组件模式:

<Table><Header/><Body/><Pagination/></Table>

3.行选中功能:支持外部控制(父组件管)和内部控制(表格自己管)两种模式

4.列排序:点表头升降序切换

5.列筛选:按文本/下拉/范围筛选

6.分页:支持外部控制，也自带默认分页UI

7.列显示/隐藏:用户可以开关列

8.批量操作:选中行后暴露给父组件全包进去	

9.抽一个useDataTable Hook，把业务逻辑(排序/筛选/分页）全包进去	

10.集成React Query:提供一个<WithQuery>外壳，直接拉后端数据。



